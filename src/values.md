# Values

```cmake
{{#include values/CMakeLists.txt}}
```

```sh
cmake -B build

cmake --build build

./build/values
```

```
<!-- cmdrun cd values && cmake -B build && cmake --build build && ./build/values -->
```
